┌─────────────────────────────────────────────────────────────────────────────────────────┐
│                     StarClassify-Vis 与 OSI 七层模型映射架构图                          │
├─────────┬──────────────┬────────────────────────────────────────────────────────────────┤
│ OSI 层级│    层级名称  │               StarClassify-Vis 系统中的具体实现                │
├─────────┼──────────────┼────────────────────────────────────────────────────────────────┤
│ Layer 7 │ 应用层       │ • 用户浏览器 (Web Browser)                                     │
│         │ (Application)│ • React 前端应用 (Lab.tsx, Runs.tsx 等页面)                    │
│         │              │ • FastAPI 业务端点 (POST /api/runs, GET /api/runs)             │
│         │              │ • PostgreSQL 数据库服务逻辑                                    │
│         │              │ • Nginx HTTP 反向代理服务                                      │
├─────────┼──────────────┼────────────────────────────────────────────────────────────────┤
│ Layer 6 │ 表示层       │ • Pydantic 模式验证 (schemas.py，数据格式化)                   │
│         │(Presentation)│ • JSON 数据的序列化与反序列化 (HTTP 响应/请求载荷)             │
│         │              │ • CSV 文件的二进制编码与解析 (pandas 处理)                     │
│         │              │ • Nginx 中可能的 HTTPS (SSL/TLS) 加密与解密                    │
├─────────┼──────────────┼────────────────────────────────────────────────────────────────┤
│ Layer 5 │ 会话层       │ • SQLAlchemy 数据库连接池 (pool_size=5, max_overflow=10)       │
│         │ (Session)    │ • 数据库事务管理 (BEGIN, COMMIT, ROLLBACK)                     │
│         │              │ • Nginx 与 FastAPI 之间的 Keep-Alive HTTP 会话                 │
│         │              │ • CORS (跨源资源共享) 会话权限控制                             │
├─────────┼──────────────┼────────────────────────────────────────────────────────────────┤
│ Layer 4 │ 传输层       │ • Nginx 监听与转发的 TCP 端口 (宿主机 8080 ↔ 容器 80)          │
│         │ (Transport)  │ • FastAPI 服务的 TCP 端口 (8000)                               │
│         │              │ • PostgreSQL 数据库 TCP 端口 (宿主机 5433 ↔ 容器 5432)         │
│         │              │ • 数据包的分割、TCP 握手与可靠传输保证                         │
├─────────┼──────────────┼────────────────────────────────────────────────────────────────┤
│ Layer 3 │ 网络层       │ • Docker 自定义网络 (starvis-network)                          │
│         │ (Network)    │ • 容器间的 IP 路由分配                                         │
│         │              │ • 容器内部 DNS 域名解析 (例如 api 容器通过主机名 "db" 找寻 PG) │
├─────────┼──────────────┼────────────────────────────────────────────────────────────────┤
│ Layer 2 │ 数据链路层   │ • Docker Bridge (docker0 或自定义网桥 veth 接口)               │
│         │ (Data Link)  │ • 宿主机与容器、容器与容器之间的虚拟 MAC 地址寻址与帧传输      │
├─────────┼──────────────┼────────────────────────────────────────────────────────────────┤
│ Layer 1 │ 物理层       │ • 承载 Docker 环境的底层宿主机 (Docker Host) 服务器            │
│         │ (Physical)   │ • 实际的 CPU、内存 (用于 GNB 训练计算)                         │
│         │              │ • 物理磁盘 I/O (持久化映射的数据卷 starvis_pgdata)             │
│         │              │ • 外部访问系统的物理网线/光纤网络                              │
└─────────┴──────────────┴────────────────────────────────────────────────────────────────┘